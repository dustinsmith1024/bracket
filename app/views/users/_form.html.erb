<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label><b>What is your name?</b></label>
    <%= f.text_field :name %>
  </div>

  <div class="field">
  <label><b>What is your favorite team? </b></label> <%= collection_select(:user, :team_id, Team.all, :id, :name, :prompt => true) %>
  </div></br><br/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for([@user,@tournament], :remote => true) do |f| %>
  <% if @tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
      <% @tournament.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <%#= collection_select(:tournament, :user_id, User.all, :id, :name, :prompt => true, :selected => @tournament.user) %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
